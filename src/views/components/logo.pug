//-----------------------------------------
//-     LOGO COMPONENT
//- 
//-     This component displays the image you pass, with a url and / or name.
//- 
//-    RECEIVES:
//-    * image {String} – The path of the image you want to display.
//-    * url {String} – The URL you want to apply to the image.
//-    * name {String} – The name of the logo.
//-
//-----------------------------------------

mixin logo(image, url, name)
  a(href=url ? url : '', title=name ? name : '', rel='noopener noreferrer').logo
    img(src=image ? image : '/img/logo--mockup.png', alt=name ? "Logotipo do" + name : '')